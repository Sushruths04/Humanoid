# Wakeboarding experiment — Python deps (Isaac Sim/Lab provided by the Docker image)
# Isaac Sim 5.1 + Isaac Lab are installed in the container; do NOT pip-install them here.
rsl-rl-lib>=2.0.0
torch>=2.7
numpy
pyyaml
tensorboard
wandb
# optional (Phase-2 CV coach / AMP video retarget) — install only when needed:
# mediapipe
# opencv-python

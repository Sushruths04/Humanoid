[project]
name = "my-humanoid-project"
version = "0.1.0"
description = "Language-conditioned G1 humanoid loco-manipulation thesis code"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
include = ["my_humanoid_project*"]

